import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Sign Up - Event Force',
  description: 'Create your Event Force account to book premium transportation and event logistics services.',
  path: '/signup',
  noIndex: true,
});
