import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Payment Successful - Event Force',
  description: 'Your Event Force booking payment was successful. Your reservation has been confirmed.',
  path: '/success',
  noIndex: true,
});
