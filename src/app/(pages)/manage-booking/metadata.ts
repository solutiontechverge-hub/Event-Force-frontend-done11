import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Manage Booking | Event Force',
  description: 'Book premium transportation with Event Force. Select your vehicle, route, and complete your reservation.',
  path: '/manage-booking',
  noIndex: true,
});
