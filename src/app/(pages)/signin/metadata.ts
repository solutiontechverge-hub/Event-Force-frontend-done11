import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Sign In - Event Force',
  description: 'Sign in to your Event Force account to manage bookings and access premium transportation services.',
  path: '/signin',
  noIndex: true,
});
