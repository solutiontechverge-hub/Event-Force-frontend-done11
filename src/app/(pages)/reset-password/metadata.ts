import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Reset Password - Event Force',
  description: 'Verify your email and reset your Event Force account password.',
  path: '/reset-password',
  noIndex: true,
});
