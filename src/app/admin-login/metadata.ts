import { createPageMetadata } from '@/lib/metadata';

export const metadata = createPageMetadata({
  title: 'Admin Login - Event Force',
  description: 'Sign in to the Event Force admin dashboard.',
  path: '/admin-login',
  noIndex: true,
});
